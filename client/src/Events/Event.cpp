#include "NovaEngine/Events/Event.hpp"

namespace NovaEngine {
    // Ce fichier reste vide pour l'instant.
    // La structure Event est intégralement définie dans le header.
    // Il est toutefois conservé pour évolutivité future (sérialisation, logs, etc.).
}